import { ChevronRight, Clock3, Filter, ListFilter } from 'lucide-react'
import { mockTimeline } from '../data/mockMissionData'
export default function MonitoringConsole({ threats, selectedThreat, onSelect }) { return <section className="monitoring-console"><div className="console-header"><div><span className="eyebrow">RISK MONITOR</span><h2>Upcoming conjunction windows</h2></div><div className="console-actions"><button><Filter size={15} /> ALL LEVELS</button><button><ListFilter size={15} /> SORT: TCA</button></div></div><div className="console-body"><div className="threat-table"><div className="table-row table-heading"><span>THREAT / OBJECT PAIR</span><span>TIME TO TCA</span><span>MISS DISTANCE</span><span>RISK</span><span /></div>{threats.map(t => <button key={t.id} type="button" onClick={() => onSelect(t)} className={`table-row threat-row ${selectedThreat.id === t.id ? 'selected' : ''}`}><span className="object-pair"><i className={`severity-dot ${t.level.toLowerCase()}`} /><b>{t.objectA}</b><em>×</em>{t.objectB}</span><span>{t.window}</span><span>{t.distance}</span><span><i className={`risk-pill ${t.level.toLowerCase()}`}>{t.level}</i></span><ChevronRight size={17} /></button>)}</div><div className="event-timeline"><span className="eyebrow">EVENT TIMELINE</span>{mockTimeline.map(e => <div className="timeline-event" key={e.time}><span className={`timeline-pin ${e.tone}`} /><time>{e.time}</time><div><strong>{e.title}</strong><small>{e.detail}</small></div></div>)}<button className="timeline-button"><Clock3 size={15} /> VIEW FULL LOG</button></div></div></section> }
